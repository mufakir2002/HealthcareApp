<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Healthcare" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowBackground">@color/bg_light</item>
    </style>

    <!-- Splash theme (use on MainActivity before content loads) -->
    <style name="Theme.Healthcare.Splash" parent="Theme.Healthcare">
        <item name="android:windowBackground">@drawable/bg_hero_gradient</item>
    </style>
</resources>
